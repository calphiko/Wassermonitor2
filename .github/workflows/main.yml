name: Create Release and Upload

on:
  push:
    branches:
      - main

jobs:
  create-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Git
        run: |
          git config --global user.name 'github-actions'
          git config --global user.email 'github-actions@github.com'

      - name: Create Release Tag
        id: create_tag
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME=v$(date +'%Y%m%d%H%M%S')
          echo "TAG_NAME=$TAG_NAME" >> $GITHUB_ENV
          git tag $TAG_NAME
          git push origin $TAG_NAME
        
      - name: Create tar.gz and tar.bz2 for Server
        run: |
          tar -czvf WM2_Server_${{ env.TAG_NAME }}.tar.gz Server
          tar -cjvf WM2_Server_${{ env.TAG_NAME }}.tar.bz2 Server

      - name: Create tar.gz and tar.bz2 for Pi
        run: |
          tar -czvf WM2_Pi_${{ env.TAG_NAME }}.tar.gz Pi
          tar -cjvf WM2_Pi_${{ env.TAG_NAME }}.tar.bz2 Pi

      - name: Generate Checksums
        run: |
          sha256sum WM2_Server_${{ env.TAG_NAME }}.tar.gz > WM2_Server_${{ env.TAG_NAME }}.tar.gz.sha256
          sha256sum WM2_Server_${{ env.TAG_NAME }}.tar.bz2 > WM2_Server_${{ env.TAG_NAME }}.tar.bz2.sha256
          sha256sum WM2_Pi_${{ env.TAG_NAME }}.tar.gz > WM2_Pi_${{ env.TAG_NAME }}.tar.gz.sha256
          sha256sum WM2_Pi_${{ env.TAG_NAME }}.tar.bz2 > WM2_Pi_${{ env.TAG_NAME }}.tar.bz2.sha256

      - name: Create index.html
        run: |
          echo "<html><body><h1>Release ${{ env.TAG_NAME }}</h1><ul>" > index.html
          echo "<li><a href='WM2_Server_${{ env.TAG_NAME }}.tar.gz'>WM2_Server_${{ env.TAG_NAME }}.tar.gz</a> (<a href='WM2_Server_${{ env.TAG_NAME }}.tar.gz.sha256'>sha256</a>)</li>" >> index.html
          echo "<li><a href='WM2_Server_${{ env.TAG_NAME }}.tar.bz2'>WM2_Server_${{ env.TAG_NAME }}.tar.bz2</a> (<a href='WM2_Server_${{ env.TAG_NAME }}.tar.bz2.sha256'>sha256</a>)</li>" >> index.html
          echo "<li><a href='WM2_Pi_${{ env.TAG_NAME }}.tar.gz'>WM2_Pi_${{ env.TAG_NAME }}.tar.gz</a> (<a href='WM2_Pi_${{ env.TAG_NAME }}.tar.gz.sha256'>sha256</a>)</li>" >> index.html
          echo "<li><a href='WM2_Pi_${{ env.TAG_NAME }}.tar.bz2'>WM2_Pi_${{ env.TAG_NAME }}.tar.bz2</a> (<a href='WM2_Pi_${{ env.TAG_NAME }}.tar.bz2.sha256'>sha256</a>)</li>" >> index.html
          echo "</ul></body></html>" >> index.html

      - name: Upload to Server
        uses: appleboy/scp-action@v0.1.1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIV_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          source: |
            WM2_Server_${{ env.TAG_NAME }}.tar.gz
            WM2_Server_${{ env.TAG_NAME }}.tar.bz2
            WM2_Pi_${{ env.TAG_NAME }}.tar.gz
            WM2_Pi_${{ env.TAG_NAME }}.tar.bz2
            WM2_Server_${{ env.TAG_NAME }}.tar.gz.sha256
            WM2_Server_${{ env.TAG_NAME }}.tar.bz2.sha256
            WM2_Pi_${{ env.TAG_NAME }}.tar.gz.sha256
            WM2_Pi_${{ env.TAG_NAME }}.tar.bz2.sha256
            index.html
          target: ${{ secrets.CODE_DEPLOY_PATH }}
